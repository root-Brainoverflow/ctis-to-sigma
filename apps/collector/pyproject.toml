[project]
name = "cts-collector"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["playwright>=1.48", "typer>=0.12", "rich>=13"]

[project.scripts]
cts = "cli:app"  # flat module 진입점

[tool.setuptools]
package-dir = {"" = "src"}
py-modules = ["cli", "link_collector"]